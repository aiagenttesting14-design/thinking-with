name: Notify Telegram on Update

on:
  push:
    branches: [ main ]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram Message
        uses: appleboy/telegram-action@master
        with:
          to: 8327567857
          token: ${{ secrets.BOT_TOKEN }}
          message: "📝 Website updated! Check it out: https://aiagenttesting14-design.github.io/thinking-with/"

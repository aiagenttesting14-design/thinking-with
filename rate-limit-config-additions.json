{
  "comment": "Merge these into openclaw.json to add rate limiting protection",
  
  "agents.defaults.subagents.spawnDelay": 3000,
  "agents.defaults.subagents.maxConcurrent": 2,
  
  "tools.web.search.rateLimit": {
    "maxPerMinute": 10,
    "delayMs": 2000
  }
}
